---
const { currentLocale } = Astro.props;

const locales = [
  { code: 'en', name: 'English', flag: '🇺🇸' },
  { code: 'tr', name: 'Türkçe', flag: '🇹🇷' },
];
---

<div class="relative">
  <button class="flex items-center space-x-2 px-3 py-2 text-sm text-gray-300 hover:text-white transition-colors"
          onmouseenter="this.nextElementSibling.classList.remove('hidden')" 
          onmouseleave="this.nextElementSibling.classList.add('hidden')">
    <span>{locales.find(l => l.code === currentLocale)?.flag}</span>
    <span>{locales.find(l => l.code === currentLocale)?.name}</span>
    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
    </svg>
  </button>
  
  <div class="absolute top-full right-0 mt-1 glass-dark rounded-lg shadow-xl hidden z-50"
       onmouseenter="this.classList.remove('hidden')" 
       onmouseleave="this.classList.add('hidden')">
    {locales.map(locale => (
      <a 
        href={locale.code === 'en' ? '/' : `/${locale.code}`}
        class={`flex items-center space-x-2 px-4 py-2 text-sm text-gray-300 hover:text-white hover:bg-white/10 transition-colors first:rounded-t-lg last:rounded-b-lg ${locale.code === currentLocale ? 'font-semibold' : ''}`}
      >
        <span>{locale.flag}</span>
        <span>{locale.name}</span>
      </a>
    ))}
  </div>
</div>
